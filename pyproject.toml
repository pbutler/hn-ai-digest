[project]
name = "hackernews-tools"
version = "0.1.0"
description = "Scripts for ingesting, ranking, and processing Hacker News articles."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "docling>=2.70.0",
    "loguru>=0.7.3",
    "markdown>=3.8",
    "openai>=2.32.0",
    "python-dotenv>=1.1.1",
    "zvec>=0.3.0",
]

[tool.uv]
package = false
